stages:
  - build
  - test
  - sonarqube



include:
  - local: 'ITSkillsNow/job-service/.gitlab-ci.yml'
  - local: 'ITSkillsNow/course-service/.gitlab-ci.yml'
  - local: 'ITSkillsNow/auth-service/.gitlab-ci.yml'
  - local: 'ITSkillsNow/user-service/.gitlab-ci.yml'


sonarqube:
  stage: sonarqube
  image: maven:3.6.3-jdk-11
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar" 
    GIT_DEPTH: "0" 
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script:
    - mvn -f ITSkillsNow/course-service clean install 
    - mvn -f ITSkillsNow/course-service/pom.xml sonar:sonar -Pcoverage
    - mvn -f ITSkillsNow/job-service clean install 
    - mvn -f ITSkillsNow/job-service/pom.xml sonar:sonar -Pcoverage
    - mvn -f ITSkillsNow/user-service clean install 
    - mvn -f ITSkillsNow/user-service/pom.xml sonar:sonar -Pcoverage
    - mvn -f ITSkillsNow/auth-service clean install 
    - mvn -f ITSkillsNow/auth-service/pom.xml sonar:sonar -Pcoverage

  allow_failure: true
  only:
    - main



